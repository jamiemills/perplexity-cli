# =============================================================================
# .semgrep-architecture.yml — structural / architectural prevention rules.
#
# All non-duplicative structural and security rules have been moved into
# .semgrep.yml (Wave 1B, Rank 17).  This file is retained for the
# coordinator to delete after Import Linter equivalence is proven for
# the retired layer-direction rules (domain-framework-import and
# application-framework-import).
#
# The baseline at quality/baselines/semgrep-architecture.json is
# referenced by scripts/check_semgrep_architecture.py which now routes
# through the canonical semgrep_policy.py wrapper.
# =============================================================================

rules:
  # ── §6: tangled dependency graph ── (MOVED to .semgrep.yml)
  # id: function-local-import — moved

  # ── §2a: retry backoff ── (MOVED to .semgrep.yml)
  # id: retry-sleep-outside-canonical — moved

  # ── §4: divergent HTTP-status classification ── (MOVED to .semgrep.yml)
  # id: ad-hoc-http-status-classification — moved

  # ── §2b: process control ── (MOVED to .semgrep.yml)
  # id: sys-exit-outside-boundary — moved

  # ── §6: HTTP-client construction ── (MOVED to .semgrep.yml)
  # id: http-client-outside-transport — moved

  # ── §2c: TOCTOU ── (MOVED to .semgrep.yml)
  # id: write-then-chmod-toctou — moved

  # ── §7: getter with side effects ── (MOVED to .semgrep.yml)
  # id: getter-with-side-effects — moved

  # ── §4: presentation leaking ── (MOVED to .semgrep.yml)
  # id: click-echo-outside-presentation — moved
