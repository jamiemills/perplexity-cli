name: Missing Permissions

on:
  push:
    branches: [main]

jobs:
  leak-secrets:
    name: Leak Secrets
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4.2.2

      - name: Do work
        run: echo "no permissions anywhere"
